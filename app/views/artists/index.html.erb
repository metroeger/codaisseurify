

<h3><bold>Artists at:<bold><h3>
<h2><strong> Codaisseurify <strong> <h2>

<% @artists.each do |artist| %>
    <div class="row">
      <div class="col-md-7">
        <h4><%=link_to artist.name, artist_path(artist.id)%></h4>
        <div class="img">
          <%= image_tag artist.photo.image %>
        </div>
      </div>
    </div>
    <% end %>

<img src="img" alt="HTML5 Icon" style="width:128px;height:128px;">
